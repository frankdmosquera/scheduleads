// Frontend: Next.js settings. Empty on purpose: the defaults are what this app wants.

import type { NextConfig } from "next";

const nextConfig: NextConfig = {/* config options here */};

export default nextConfig;
